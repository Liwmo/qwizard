app.controller('finished-quiz', 'quizFactory', 'resultsFactory', '$routeParams', ['$scope', function($scope, quizFactory, resultsFactory, $routeParams) {

    // $scope.average;
    // $scope.max;
    // $scope.dates;
    
    // console.log($routeParams.id);
    // var quiz = quizFactory.getLiveQuiz($routeParams.id);

}]);
